# Implementation Status

This file tracks **every** backlog task.

- **Task index** (below, generated) — all backlog tasks with status, an owner
  placeholder, and an evidence link. Generated by `scripts/task_tracking.py`;
  do not hand-edit the structural columns (Title/Epoch/Depends).
- **Detailed evidence log** — the full evidence narrative for active/completed tasks.

Rules:

- Status lifecycle: **Not started → In progress → In review → Complete** (or **Blocked**).
- **Status changes require acceptance evidence:** a task may enter In progress / In
  review / Complete only with an evidence link (commit, CI run, or a Detailed evidence
  log entry). Enforced by `scripts/task_tracking.py --check` (run by `make check-tracking`
  and `make ci`).
- A task is **Complete** only when every acceptance criterion passes.
- **Owner** is a placeholder (`_unassigned_`) until assigned.
- After editing `docs/BACKLOG.md`, run `python3 scripts/task_tracking.py --generate`.

## Task index

<!-- BEGIN TASK INDEX (generated by scripts/task_tracking.py) -->
| Task | Title | Epoch | Depends | Status | Owner | Evidence |
|---|---|---|---|---|---|---|
| WG-001 | Freeze MVP scope and terminology | E00 | None | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-002 | Create ADR index and make initial platform decisions | E00 | WG-001 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-003 | Create repository structure and Xcode project | E00 | WG-002 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-004 | Add formatting, linting, and warning policy | E00 | WG-003 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-005 | Create CI build and unit-test workflow | E00 | WG-003 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-006 | Create implementation status and task tracking | E00 | WG-001 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-007 | Create deterministic test-support module | E00 | WG-003 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-008 | Create threat model and abuse-case register | E00 | WG-001 | Not started | _unassigned_ | — |
| WG-010 | Define alarm domain models | E01 | WG-003 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-011 | Define command, proposal, and audit models | E01 | WG-010 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-012 | Define repository protocols | E01 | WG-010 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-013 | Select and configure persistence | E01 | WG-002, WG-012 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-014 | Implement alarm repository | E01 | WG-013 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-015 | Implement append-only audit repository | E01 | WG-013 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-016 | Implement external-operation outbox | E01 | WG-013 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-017 | Add schema migration test harness | E01 | WG-013 | Not started | _unassigned_ | — |
| WG-018 | Add dependency container and environment composition | E01 | WG-012 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-019 | Add privacy-safe structured logging | E01 | WG-008 | Not started | _unassigned_ | — |
| WG-020 | Implement pure next-occurrence calculator | E02 | WG-010, WG-007 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-021 | Implement wall-clock versus fixed-zone semantics | E02 | WG-020 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-022 | Handle DST ambiguous and nonexistent times | E02 | WG-021 | Not started | _unassigned_ | — |
| WG-023 | Handle International Date Line and unusual offsets | E02 | WG-021 | Not started | _unassigned_ | — |
| WG-024 | Define AlarmKit adapter protocol and fake | E02 | WG-010 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-025 | Implement AlarmKit authorization flow | E02 | WG-024 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-026 | Implement AlarmKit schedule mapping | E02 | WG-020, WG-024 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-027 | Implement transactional alarm command processor | E02 | WG-014, WG-015, WG-016, WG-026 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-028 | Implement alarm policy engine | E02 | WG-011 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-029 | Implement launch and foreground reconciliation | E02 | WG-027 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-030 | Implement schedule/cancel/snooze integration tests | E02 | WG-027 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-031 | Create real-device AlarmKit smoke-test checklist | E02 | WG-026 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-040 | Create design tokens and reusable components | E03 | WG-003 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-041 | Build alarm list and next-alarm summary | E03 | WG-014, WG-040 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-042 | Build create-alarm flow | E03 | WG-027, WG-040 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-043 | Build edit, enable, disable, and delete flows | E03 | WG-041, WG-042 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-044 | Build critical alarm configuration | E03 | WG-028, WG-042 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-045 | Build challenge configuration UI | E03 | WG-042 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-046 | Build travel policy UI | E03 | WG-021, WG-042 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-047 | Build pre-alarm policy UI | E03 | WG-042 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-048 | Build alarm history and audit detail UI | E03 | WG-015, WG-040 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-049 | Add deep links for alarm and proposal screens | E03 | WG-041 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-050 | Add UI tests for core alarm flows | E03 | WG-041, WG-049 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-060 | Define normalized motion source protocols | E04 | WG-007 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-061 | Implement Motion & Fitness permission flow | E04 | WG-060 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-062 | Implement historical pedometer adapter | E04 | WG-060 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-063 | Implement live pedometer adapter | E04 | WG-060 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-064 | Implement motion activity adapter | E04 | WG-060 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-065 | Implement device-carried and pickup evidence | E04 | WG-060 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-066 | Implement optional altimeter evidence | E04 | WG-060 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-067 | Implement movement episode builder | E04 | WG-062, WG-063, WG-064, WG-065 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-068 | Implement wake challenge state machine | E04 | WG-067 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-069 | Implement ten-second walking verification | E04 | WG-068 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-070 | Implement anti-shake and replay defenses | E04 | WG-069 | Complete | _unassigned_ | [detail](#detailed-evidence-log) |
| WG-071 | Implement challenge UI and progress feedback | E04 | WG-045, WG-069 | Not started | _unassigned_ | — |
| WG-072 | Implement accessible challenge alternatives | E04 | WG-068 | Not started | _unassigned_ | — |
| WG-073 | Connect valid challenge pass to authorized stop | E04 | WG-027, WG-071 | Not started | _unassigned_ | — |
| WG-074 | Build motion trace recorder for internal testing | E04 | WG-060 | Not started | _unassigned_ | — |
| WG-075 | Run real-device calibration study | E04 | WG-074 | Not started | _unassigned_ | — |
| WG-080 | Define deterministic awake-evidence model | E05 | WG-067 | Not started | _unassigned_ | — |
| WG-081 | Implement recent movement history query | E05 | WG-062 | Not started | _unassigned_ | — |
| WG-082 | Implement pre-alarm evaluator | E05 | WG-080, WG-081 | Not started | _unassigned_ | — |
| WG-083 | Implement pre-alarm notification categories and actions | E05 | WG-082 | Not started | _unassigned_ | — |
| WG-084 | Implement keep-original action | E05 | WG-083 | Not started | _unassigned_ | — |
| WG-085 | Implement turn-off-today action | E05 | WG-083, WG-028 | Not started | _unassigned_ | — |
| WG-086 | Implement change-time action | E05 | WG-083, WG-049 | Not started | _unassigned_ | — |
| WG-087 | Implement remind-later action | E05 | WG-083 | Not started | _unassigned_ | — |
| WG-088 | Implement pre-alarm background opportunity handler | E05 | WG-082 | Not started | _unassigned_ | — |
| WG-089 | Implement foreground fallback evaluation | E05 | WG-082 | Not started | _unassigned_ | — |
| WG-090 | Add false-positive feedback loop | E05 | WG-083 | Not started | _unassigned_ | — |
| WG-091 | Adversarial test bathroom-return-to-bed scenario | E05 | WG-082 | Not started | _unassigned_ | — |
| WG-100 | Implement system time-zone observer | E06 | WG-021 | Not started | _unassigned_ | — |
| WG-101 | Implement travel context domain model | E06 | WG-100 | Not started | _unassigned_ | — |
| WG-102 | Implement low-power significant-location adapter | E06 | WG-101 | Not started | _unassigned_ | — |
| WG-103 | Implement optional region-monitoring rules | E06 | WG-101 | Not started | _unassigned_ | — |
| WG-104 | Implement travel policy evaluator | E06 | WG-100, WG-101 | Not started | _unassigned_ | — |
| WG-105 | Build time-zone change prompt | E06 | WG-104, WG-049 | Not started | _unassigned_ | — |
| WG-106 | Handle DST transition during travel | E06 | WG-022, WG-104 | Not started | _unassigned_ | — |
| WG-107 | Handle International Date Line travel | E06 | WG-023, WG-104 | Not started | _unassigned_ | — |
| WG-108 | Handle airport/transit and rapid zone changes | E06 | WG-104 | Not started | _unassigned_ | — |
| WG-109 | Build location permission education and controls | E06 | WG-102, WG-040 | Not started | _unassigned_ | — |
| WG-110 | Create travel real-device/manual simulation matrix | E06 | WG-105 | Not started | _unassigned_ | — |
| WG-120 | Define wellness data minimization plan | E07 | WG-008 | Not started | _unassigned_ | — |
| WG-121 | Implement contextual HealthKit authorization | E07 | WG-120 | Not started | _unassigned_ | — |
| WG-122 | Implement sleep-analysis query adapter | E07 | WG-121 | Not started | _unassigned_ | — |
| WG-123 | Implement sleep-duration and consistency calculator | E07 | WG-122 | Not started | _unassigned_ | — |
| WG-124 | Implement conservative sleep-debt estimate | E07 | WG-123 | Not started | _unassigned_ | — |
| WG-125 | Implement readiness factor model | E07 | WG-123 | Not started | _unassigned_ | — |
| WG-126 | Build readiness explanation UI | E07 | WG-125, WG-040 | Not started | _unassigned_ | — |
| WG-127 | Implement evidence-based habit suggestion library | E07 | WG-125 | Not started | _unassigned_ | — |
| WG-128 | Add wellness disclaimers and safety copy | E07 | WG-126 | Not started | _unassigned_ | — |
| WG-129 | Implement health data export/delete controls | E07 | WG-120 | Not started | _unassigned_ | — |
| WG-130 | Test partial/denied/revoked HealthKit access | E07 | WG-121, WG-129 | Not started | _unassigned_ | — |
| WG-140 | Define calendar data minimization and redaction | E08 | WG-008 | Not started | _unassigned_ | — |
| WG-141 | Implement contextual EventKit authorization | E08 | WG-140 | Not started | _unassigned_ | — |
| WG-142 | Implement upcoming-event adapter | E08 | WG-141 | Not started | _unassigned_ | — |
| WG-143 | Implement user-confirmed critical event marking | E08 | WG-142 | Not started | _unassigned_ | — |
| WG-144 | Implement morning preparation profile | E08 | WG-010 | Not started | _unassigned_ | — |
| WG-145 | Implement latest-safe-wake calculator | E08 | WG-142, WG-144 | Not started | _unassigned_ | — |
| WG-146 | Build tomorrow-plan recommendation screen | E08 | WG-145, WG-040 | Not started | _unassigned_ | — |
| WG-147 | Implement calendar-change refresh | E08 | WG-142, WG-146 | Not started | _unassigned_ | — |
| WG-148 | Test hostile or misleading event text | E08 | WG-140, WG-142 | Not started | _unassigned_ | — |
| WG-160 | Define LanguageModelProvider protocol and fake | E09 | WG-011 | Not started | _unassigned_ | — |
| WG-161 | Define structured schemas for AI use cases | E09 | WG-160 | Not started | _unassigned_ | — |
| WG-162 | Implement Apple Foundation Models availability gate | E09 | WG-160 | Not started | _unassigned_ | — |
| WG-163 | Implement on-device structured generation adapter | E09 | WG-161, WG-162 | Not started | _unassigned_ | — |
| WG-164 | Implement natural-language alarm parser | E09 | WG-163 | Not started | _unassigned_ | — |
| WG-165 | Implement deterministic alarm intent validator | E09 | WG-164 | Not started | _unassigned_ | — |
| WG-166 | Build conversational alarm creation UI | E09 | WG-164, WG-165 | Not started | _unassigned_ | — |
| WG-167 | Implement Tomorrow Agent context builder | E09 | WG-125, WG-145 | Not started | _unassigned_ | — |
| WG-168 | Implement Tomorrow Agent proposal generation | E09 | WG-163, WG-167 | Not started | _unassigned_ | — |
| WG-169 | Implement explanation generator grounded in factor IDs | E09 | WG-163 | Not started | _unassigned_ | — |
| WG-170 | Implement conversational sleep-journal extraction | E09 | WG-163 | Not started | _unassigned_ | — |
| WG-171 | Implement agent permission settings | E09 | WG-028, WG-040 | Not started | _unassigned_ | — |
| WG-172 | Implement AgentOrchestrator and policy handoff | E09 | WG-168, WG-171 | Not started | _unassigned_ | — |
| WG-173 | Implement prompt-injection defenses | E09 | WG-148, WG-172 | Not started | _unassigned_ | — |
| WG-174 | Implement optional cloud-provider interface behind feature flag | E09 | WG-160, WG-120 | Not started | _unassigned_ | — |
| WG-175 | Create AI evaluation corpus | E09 | WG-161 | Not started | _unassigned_ | — |
| WG-176 | Run AI hallucination and safety evaluation | E09 | WG-175 | Not started | _unassigned_ | — |
| WG-180 | Build permission and consent center | E10 | WG-040 | Not started | _unassigned_ | — |
| WG-181 | Implement sensitive-data classification types | E10 | WG-019 | Not started | _unassigned_ | — |
| WG-182 | Implement local data retention controls | E10 | WG-013 | Not started | _unassigned_ | — |
| WG-183 | Implement full local export | E10 | WG-012 | Not started | _unassigned_ | — |
| WG-184 | Implement accountless local deletion/reset | E10 | WG-182 | Not started | _unassigned_ | — |
| WG-185 | Audit Keychain and secret handling | E10 | WG-174 | Not started | _unassigned_ | — |
| WG-186 | Create privacy manifest and SDK inventory | E10 | WG-003 | Not started | _unassigned_ | — |
| WG-187 | Draft App Privacy Nutrition Label mapping | E10 | WG-120, WG-140, WG-186 | Not started | _unassigned_ | — |
| WG-188 | Draft in-app and web privacy policy requirements | E10 | WG-187 | Not started | _unassigned_ | — |
| WG-189 | Run static security and dependency audit | E10 | WG-185, WG-186 | Not started | _unassigned_ | — |
| WG-190 | Run privacy leak test on logs and analytics | E10 | WG-181 | Not started | _unassigned_ | — |
| WG-191 | Prepare App Review notes and demo mode | E10 | WG-188 | Not started | _unassigned_ | — |
| WG-200 | Implement progressive onboarding | E11 | WG-040, WG-180 | Not started | _unassigned_ | — |
| WG-201 | Complete VoiceOver labels, values, hints, and focus order | E11 | WG-041 | Not started | _unassigned_ | — |
| WG-202 | Complete Dynamic Type and layout stress test | E11 | WG-040 | Not started | _unassigned_ | — |
| WG-203 | Implement Reduce Motion and haptic alternatives | E11 | WG-071 | Not started | _unassigned_ | — |
| WG-204 | Complete contrast and non-color status audit | E11 | WG-040 | Not started | _unassigned_ | — |
| WG-205 | Implement 12/24-hour and locale-aware formatting | E11 | WG-020 | Not started | _unassigned_ | — |
| WG-206 | Externalize and localize all user-facing strings | E11 | WG-083 | Not started | _unassigned_ | — |
| WG-207 | Test right-to-left layout | E11 | WG-206 | Not started | _unassigned_ | — |
| WG-208 | Run sleep-inertia usability review | E11 | WG-071, WG-200 | Not started | _unassigned_ | — |
| WG-209 | Run aesthetic consistency epoch | E11 | WG-041, WG-208 | Not started | _unassigned_ | — |
| WG-210 | Run device-size and orientation visual regression | E11 | WG-209 | Not started | _unassigned_ | — |
| WG-220 | Implement coarse privacy-safe analytics abstraction | E12 | WG-019, WG-187 | Not started | _unassigned_ | — |
| WG-221 | Implement crash diagnostics redaction | E12 | WG-181 | Not started | _unassigned_ | — |
| WG-222 | Measure cold-launch and reconciliation performance | E12 | WG-029 | Not started | _unassigned_ | — |
| WG-223 | Measure overnight battery baseline | E12 | WG-063, WG-088, WG-102 | Not started | _unassigned_ | — |
| WG-224 | Optimize motion challenge battery and responsiveness | E12 | WG-223 | Not started | _unassigned_ | — |
| WG-225 | Optimize location and background scheduling | E12 | WG-223 | Not started | _unassigned_ | — |
| WG-226 | Stress-test alarm command concurrency | E12 | WG-027 | Not started | _unassigned_ | — |
| WG-227 | Stress-test persistence corruption and recovery | E12 | WG-017, WG-029 | Not started | _unassigned_ | — |
| WG-228 | Run 100-cycle alarm/challenge soak test | E12 | WG-073 | Not started | _unassigned_ | — |
| WG-229 | Run background expiration and termination tests | E12 | WG-088, WG-029 | Not started | _unassigned_ | — |
| WG-230 | Build operational diagnostics screen | E12 | WG-029, WG-221 | Not started | _unassigned_ | — |
| WG-240 | Epoch 1: independent architecture and invariant review | E13 | WG-030, WG-075 | Not started | _unassigned_ | — |
| WG-241 | Epoch 2: functional bug hunt | E13 | WG-240 | Not started | _unassigned_ | — |
| WG-242 | Epoch 3: time, DST, recurrence, and travel red team | E13 | WG-110 | Not started | _unassigned_ | — |
| WG-243 | Epoch 4: motion spoofing and false inference red team | E13 | WG-075, WG-091 | Not started | _unassigned_ | — |
| WG-244 | Epoch 5: background, reboot, permission, and race chaos | E13 | WG-229 | Not started | _unassigned_ | — |
| WG-245 | Epoch 6: AI prompt injection and hallucination red team | E13 | WG-176 | Not started | _unassigned_ | — |
| WG-246 | Epoch 7: privacy and security review | E13 | WG-190 | Not started | _unassigned_ | — |
| WG-247 | Epoch 8: accessibility review with assistive settings | E13 | WG-201, WG-207 | Not started | _unassigned_ | — |
| WG-248 | Epoch 9: aesthetic and interaction polish | E13 | WG-209, WG-210 | Not started | _unassigned_ | — |
| WG-249 | Epoch 10: battery and performance regression | E13 | WG-222, WG-225 | Not started | _unassigned_ | — |
| WG-250 | Epoch 11: App Store compliance preflight | E13 | WG-191, WG-246 | Not started | _unassigned_ | — |
| WG-251 | Epoch 12: release-candidate bug bash | E13 | WG-241, WG-250 | Not started | _unassigned_ | — |
| WG-252 | Run final regression after fixes | E13 | WG-251 | Not started | _unassigned_ | — |
| WG-260 | Create internal TestFlight build pipeline | E14 | WG-005, WG-250 | Not started | _unassigned_ | — |
| WG-261 | Recruit staged beta cohorts and consent | E14 | WG-260 | Not started | _unassigned_ | — |
| WG-262 | Run two-week alarm reliability beta | E14 | WG-261 | Not started | _unassigned_ | — |
| WG-263 | Run travel and time-zone beta | E14 | WG-261 | Not started | _unassigned_ | — |
| WG-264 | Run motion challenge calibration beta | E14 | WG-261 | Not started | _unassigned_ | — |
| WG-265 | Run AI usefulness and safety beta | E14 | WG-261, WG-176 | Not started | _unassigned_ | — |
| WG-266 | Finalize support, FAQ, privacy, and safety pages | E14 | WG-188, WG-262 | Not started | _unassigned_ | — |
| WG-267 | Finalize App Store metadata and screenshots | E14 | WG-248, WG-266 | Not started | _unassigned_ | — |
| WG-268 | Submit release candidate and handle review feedback | E14 | WG-252, WG-267 | Not started | _unassigned_ | — |
| WG-269 | Prepare rollback and smart-feature kill-switch plan | E14 | WG-220, WG-268 | Not started | _unassigned_ | — |
| WG-270 | Post-release monitoring and incident process | E14 | WG-268 | Not started | _unassigned_ | — |
| WG-271 | Thirty-day post-launch review | E14 | WG-270 | Not started | _unassigned_ | — |
<!-- END TASK INDEX -->

## Detailed evidence log

Status/owner/evidence are authoritative in the Task index above; this log holds the
detailed evidence narrative for active and completed tasks.

| Task | Status | Branch/Commit | Automated Evidence | Manual Evidence | Notes |
|---|---|---|---|---|---|
| WG-001 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | N/A — docs-only change. No build/test target yet (created in WG-003/WG-007); available suite = 0 tests. | `docs/SCOPE.md` created: scope-freeze + sign-off block (§1), glossary defining the four named terms (critical alarm, occurrence, local time, fixed-zone time) plus the supporting alarm/scheduling/time/AI/audit terms (§2.2–§2.5), explicit in-scope (§3) and out-of-scope (§4). `alarm-safety-reviewer` pass: no safety-invariant weakening; two definitions stricter than source (#6 "suppressed", #31 AI-inference ban). Review fixes M-1/M-2/L-1/L-2/N-1 applied. A 3-round/6-lens multi-agent adversarial review (2026-08-01) confirmed no safety weakening and applied 7 further docs-accuracy fixes (report: `docs/reviews/2026-08-01-wg001-scope-terminology.md`). | All acceptance criteria pass. Product owner (yeshwanth devabhaktuni) countersigned the `SCOPE.md §1` freeze on 2026-08-01. Operational "critical alarm" defaults deferred to ADR-007 (indexed in `docs/DECISIONS.md`; unscheduled). |
| WG-002 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | N/A — docs-only change. No build/test target yet (WG-003/WG-007); available suite = 0 tests. | `docs/DECISIONS.md` records ADR-001 (deployment target → iOS 26+), ADR-002 (persistence → Core Data primary, SwiftData deferred), ADR-003 (modularization → single target now, extract packages later), ADR-004 (MVP AI → on-device only). Each ADR states Status/Date/Context/Decision/Alternatives/Consequences/Safety-privacy impact/Revisit trigger. `ios-architect` review pass; findings applied. WG-002 assumptions recorded in the DECISIONS.md assumptions log. | All acceptance criteria pass (ADRs cover deployment target, persistence, modularization, AI provider; each has tradeoffs + revisit trigger). ADR-002 (Core Data-primary) is the one new consequential commitment — human-ratified 2026-08-01; amendable behind repository protocols. No `ARCHITECTURE.md` edits (out of scope). |
| WG-003 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make test` green on iPhone 17 / iOS 26.5 simulator — `xcodegen generate` + `xcodebuild build` + 2/2 unit tests (`ScaffoldSmokeTests`) pass; Swift 6, `SWIFT_STRICT_CONCURRENCY=complete`, 0 compiler warnings. | Manual real-device launch pending (placeholder shell only — no alarm/motion/time-zone/notification behavior yet). | XcodeGen project (`project.yml`; generated `.xcodeproj` git-ignored) — single `WakeGuard` app target (iOS 26), 16 feature folders mirroring `ARCHITECTURE.md` §2, `@main` entry point free of business logic (ADR-003). `ios-architect` review pass; findings H1/M1/M2/L1 applied. Warnings-as-errors + import lint deferred to WG-004; `TestSupport` empty until WG-007. |
| WG-004 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci` green on iPhone 17 / iOS 26.5 — `swift format lint --strict` exit 0, `swiftlint --strict` 0 violations / 19 files, build + 2/2 tests, 0 warnings (warnings-as-errors enforced). Custom `domain_no_apple_frameworks` (+ `canImport` variant) proven: 6 forbidden-import forms — incl. `import struct X.Y`, `@_exported`, `@preconcurrency`, `canImport(...)`, and `import AlarmKit` in `AIApplication` — all fail the lint, then revert clean. | None required (tooling-only change; no alarm/motion/time-zone/notification behavior). | `.swiftlint.yml` (force-unwrap/try/cast = error; Domain+Application purity rules), `.swift-format`, `project.yml` warnings-as-errors, `Makefile` (`lint`/`format`/`format-check`/`ci`), README policy docs. `ios-architect` review pass; H1/H2/M1/M2/L1/L2/N2 applied (L3 tools-check deferred to WG-005). Force-unwrap is an error in tests too (intentional; use `XCTUnwrap`). |
| WG-005 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | **Observed green CI run** on GitHub Actions `macos-latest`: run 30710911477, 3m20s, all steps ✓ — Xcode 26 + iOS 26 SDK asserted, `make ci` (swiftlint 0 / swift-format clean / 2/2 tests), `TestResults.xcresult` uploaded. Plus `actionlint` clean and local `make ci` green. | None — CI observed green. Two benign annotations: GitHub Node20 action-runtime deprecation (folded into the SHA-pin follow-up) and a runner `aws/tap` Homebrew notice; neither affects the build. | GitHub Actions (`.github/workflows/ci.yml`) on push-main/PR: `macos-latest`, selects Xcode 26 + **asserts the iOS 26 SDK/formatter**, no secrets (`contents: read`), uploads `TestResults.xcresult`. Reuses `make ci` (no local/CI drift). `release-test-engineer` review pass; B1/H1/H2/M1/M2/L3/N1 applied; L1/L2 (pin image / SHA-pin+bump actions) recorded as follow-ups. |
| WG-006 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci` green (now gated on `check-tracking`): 176/176 tasks tracked, 2/2 tests, swiftlint 0, swift-format clean. `scripts/task_tracking.py` proven: `--generate` emits 176 rows; `--check` fails on a missing task, a Complete task with junk/no evidence, structural drift, and duplicate markers — then passes when restored. | None (docs/tooling; no alarm/motion/time-zone/notification behavior). | Restructured this file into policy + generated **Task index** (all 176 tasks: status, `_unassigned_` owner placeholder, evidence link) + **Detailed evidence log**. New `scripts/task_tracking.py` (stdlib-only) generates the index and enforces coverage + evidence-on-status-change; wired into `make ci`. `release-test-engineer` review pass; H1 (evidence must look like a link) / H2 (structural stale-check) / M1 (duplicate-marker guard) / N1 applied. |
| WG-007 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci` green: 9 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. Determinism + thread-safety proven — incl. 1,000 concurrent `next()` (all unique) and 500 concurrent `upsert` (all land). | None (test-support only; no product/alarm/motion/time-zone/notification behavior). | Added `Tests/TestSupport/`: `WallClock`+`TestClock`, `IdentifierGenerator`+`DeterministicIDGenerator`, generic `InMemoryRepository<Element>`, `Synchronized` (Mutex). Ports are test-scoped pending promotion (WG-018/020, ADR-003); domain-dependent fakes deferred to WG-010/012/024/060. `ios-architect` review pass (compiled against the real SDK); L1/L2 applied (`WallClock` rename) + 3 stress/ordering tests added. |
| WG-010 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci` green: 23 tests pass (0 failures), 0 warnings, swiftlint 0 (incl. domain-purity rule), swift-format clean, tracking OK. Validation proven at construction *and* decode (adversarial JSON), incl. the #11 GMT-offset family. | None (domain models only; no alarm/motion/time-zone/notification runtime behavior). | Added `Sources/AlarmDomain/`: validated leaf types (`TimeOfDay`, `CalendarDate`, `Weekday`/`WeekdaySet`, `IANATimeZone`), `ScheduleRule` (oneTime/weekly), the policies (Criticality, AlarmSound, Snooze/PreAlarm/Travel/Challenge), and the `Alarm` aggregate — all Codable/Sendable, invalid states unrepresentable-or-validated. Foundation-only (domain purity). `ios-architect` review (run vs the real SDK): fixed a #11 blocker (bare-`GMT` hole) + added GMT-family/composite-decode tests. Alias-equality limitation + empty-string fields recorded in DECISIONS. |
| WG-011 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci` green: 31 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. Command/proposal separation, Codable round-trips, proposal expiry, and fail-safe unknown actor/source/outcome/confidence/evidence decode all verified. | None (domain models only; no runtime behavior). | Added `AlarmCommand`, `AlarmProposal` (+ ConfidenceBand/EvidenceKind/EvidenceReference/ModelProviderMetadata), `AuditEvent` (+ AuditActor/CommandSource/Outcome/CorrelationID/AuditEventID). AI proposals are a distinct, non-executable type (#4/#5); unknown enum values fail-closed (#27); audit captures actor/before-after/reason/outcome (#46–#50). `alarm-safety-reviewer` pass (verified vs the real SDK incl. DST occurrence identity): no Blocker/High; M1/M2/Nit applied; downstream handoffs (allow-list AI-proposable commands, revision reconciliation) recorded in DECISIONS. |
| WG-020 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci` green: 44 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. One-time + weekly determinism, month/year boundaries, clock + zone injection, and DST characterization (spring-forward resolves forward — not skipped; fall-back returns the earlier instant) all verified. | None (pure calculation; no consumer wired yet). | Added `AlarmSchedulingEngine` — pure `nextOccurrence(of:after:in:)`, Foundation-only, `> now` semantics; `.nextTime` DST default never skips (safe vs `.strict`). `alarm-safety-reviewer` pass (SDK-verified DST + fencepost): no Blocker/High; 3 DST characterization tests added; handoffs to WG-021/022/026/029 (zone-selection choke point, converge one-time/weekly gap, dedupe fall-back, never cancel a live ring) recorded in DECISIONS. swiftlint `opening_brace` disabled (swift-format owns brace placement). |
| WG-021 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci` green: 51 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. Follow-local (device zone) vs fixed (anchor zone) distinct instants; `askOnChange`/`regionRule` → anchor safe default; IANA anchor persists through Codable. | None (pure zone selection; no runtime behavior). | Added `AlarmSchedulingEngine+TravelBehavior`: `schedulingTimeZone(for:anchor:deviceTimeZone:)` — the WG-020 zone-selection choke point (#12) — plus `nextOccurrence(for: Alarm, after:, deviceTimeZone:)`. #16 no-silent-shift (ask/region default to anchor; no device *location* read). `alarm-safety-reviewer` pass (SDK-verified): no Blocker/High; Low/Nit doc notes applied; E06 must route region resolution through this choke point (recorded in DECISIONS). |
| WG-012 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci` green: 57 tests pass (0 failures), 0 warnings, swiftlint 0 (domain-purity rule covers the new files), swift-format clean, tracking OK. Each protocol exercised via in-test actor fakes; outbox lifecycle + fail-closed decode verified. | None (protocols + value types; implementations are WG-013–016). | Added `Repositories.swift` (`AlarmRepository` / `AuditRepository` [append-only, no update/delete] / `SettingsRepository` / `OutboxRepository` — async throws, Sendable, **no Apple-framework types**), `AppSettings` (privacy-first flags), and `OutboxEntry` + `OutboxStatus` (pending/inProgress/applied/uncertain/failed) + `OutboxEntryID`. `ios-architect` review pass (no Blocker/High): shaped the outbox port for its WG-016 consumer (added `uncertain` state, `unresolvedEntries()`, `entry(idempotencyKey:)`); doc handoffs (optimistic-concurrency `save`, coarse failure reason #41, ADR-009 pagination) recorded in DECISIONS. |
| WG-013 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci` green: 62 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. In-memory + Core Data share one `SettingsRepository` contract; schema versioned; Core Data persists across instances; concurrent-save keeps a single row (B1 fix). | None (persistence foundation; alarm/audit/outbox repos are WG-014–016, migration harness WG-017; on-device only). | Added `PersistenceController` (programmatic versioned `NSManagedObjectModel`, in-memory/on-disk, history tracking on both, file protection, `@unchecked Sendable` justified on the container owner) + `CoreDataSettingsRepository` / `InMemorySettingsRepository` (shared-contract proof) — all in `AlarmInfrastructure` (no domain leak). `ios-architect` review (SDK-verified) found a **Blocker** (concurrent-save duplicate rows / stale reads) — fixed with a `singletonKey` uniqueness constraint + last-writer-wins merge policy + a concurrency test; H2 (no-op viewContext merge removed) / M1 (in-memory history tracking) applied; WG-014/017 handoffs (opposite merge policy, heavyweight-migration spike) recorded in DECISIONS. |
| WG-014 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci` green: 66 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. CRUD + optimistic revision (typed `staleRevision` rejection, stored value unchanged) + an 8-way concurrent test (exactly 1 winner; every loser a typed error — no silent overwrite, #10). | None (Core Data alarm repo; audit/outbox are WG-015/016; delete revision-guarding is WG-027). | Added `CoreDataAlarmRepository` (actor, `NSMergePolicy.error`; app-level revision guard + store-level conflict → typed `AlarmRepositoryError`) and the `AlarmRecord` entity (schema → v2). `ios-architect` review (SDK-verified: two-context race, error codes, KVC Int64 read, v1→v2 lightweight migration): no Blocker/High. M1 (conflict-path test hardened) / L2 (named `NSManagedObject*` constants) / L3 (delete save wrapped) / N5 (error labels `stored:incoming:`) applied; delete-not-revision-guarded handoff to WG-027 in DECISIONS. |
| WG-015 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci` green: 73 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. Append-only insert, idempotent on id (8-way same-id race → 1 row, no throw; 8 distinct → all persist); `(timestamp,id)` ordering; one undecodable row is skipped not fatal (#49); a `.create` audit excludes #41 enumerated categories and hashes state. | On-device only; `Alarm.label` is stored verbatim in `.create`/`.update` audit payloads (accepted MVP; redaction boundary → WG-027). Same throwing-decode pattern in `CoreDataAlarmRepository` (WG-014) flagged as a follow-up. Cross-instance audit persistence → WG-017 (manual/real-device). | Added `CoreDataAuditRepository` (actor; insert-only + idempotent via `id` uniqueness + `NSMergePolicy.error` + rollback; resilient reads) and the `AuditRecord` entity (schema → v3). Reviews: `alarm-safety-reviewer` + `ios-architect` + `privacy-security-reviewer` (SDK/graph-verified, no Blocker). Applied: decode-resilience MAJOR (skip corrupt rows, +test); corrected 3 overstated sensitive-exclusion comments and replaced the strawman `.snooze` test with an honest `.create`/label test; scoped append-only wording to the port; tightened the distinct-append test; added the id-uniqueness precondition note. Deferred (DECISIONS handoffs): label redaction + `userVisibleReason` vocabulary (WG-027), corrupt-row count surfacing, alarm-repo decode fix, tamper-evidence ADR, `versionIdentifiers` framing (WG-017). |
| WG-016 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci` green: 84 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. Full lifecycle (pending/inProgress/applied/uncertain/failed); idempotent enqueue on key incl. 8-way concurrent race → 1 row; recovery via `unresolvedEntries` across a fresh repo; bounded retry (exactly maxAttempts then `retryLimitExceeded` + `.failed`); terminal-not-resurrected; concurrent `markInProgress` → attempts == winners (no lost update). | `.failed`/`uncertain`/undecodable-row recovery is delegated to WG-029's independent AlarmKit divergence scan (not the outbox); single command-serialization boundary + label redaction are WG-027 (DECISIONS handoffs). Cross-instance persistence → WG-017. | Added `CoreDataOutboxRepository` (actor; idempotent enqueue via `idempotencyKey` uniqueness + normalize-to-pending; guarded pure state machine; bounded retries; resilient reads) and the `OutboxRecord` entity (schema → v4). Reviews: `alarm-safety-reviewer` + `ios-architect` (both SDK-verified, no Blocker; ios-architect confirmed the WG-013 race does not recur on either the insert or read-modify-write path). Applied: reworded overstated doc comments, enqueue normalization (defense in depth), and the missing `mark*` concurrency test. Two MAJORs (terminal-`.failed` invisibility; `uncertain`-at-cap #10) are recovery-semantics delegations to WG-029, recorded in DECISIONS. |
| WG-018 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci` green: 90 tests pass (0 failures), 0 warnings, swiftlint 0 (incl. the new `domain_no_composition_root` rule; fire-verified with a domain probe), swift-format clean, tracking OK. In-memory graph round-trips all 4 repos + settings; injected clock/ids are deterministic; two graphs are isolated; live adapters read real time / unique ids; container is Sendable (compile guard). | On-device only. `production()` writes the real on-disk store, so it's exercised at app launch, not unit-tested (a live-adapter/default assertion covers the wiring disk-free). Main-thread launch load is an accepted MVP tradeoff (watchdog risk on a future heavyweight migration → WG-017). Composition-error UI is minimal; full a11y audit is E08. | Added `AppEnvironment` (Sendable composition root; explicit `production()`/`inMemory()` graphs; no service locator) + live `SystemClock`/`SystemIdentifierGenerator` in `Sources/AppComposition/`; promoted `WallClock`/`IdentifierGenerator` ports into `AlarmDomain` (fakes stay in TestSupport via `@testable`); wired `WakeGuardApp` (build production, inject `\.appEnvironment`, honest `CompositionErrorView` on failure) with a fakes-backed `RootView` preview; added the `domain_no_composition_root` lint rule. Review: `ios-architect` (SDK-verified, no Blocker). Applied: MAJOR-1 (test no longer writes the real on-disk store), MAJOR-2 (optional key + documented nil-contract after a fataling default proved to crash at scene setup), + minors (lint widen/verify, Sendable guard, watchdog note, softened error copy). Package-extraction + consumer handoffs in DECISIONS. |
| WG-024 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci` green: 103 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. 13 adapter tests: schedule/cancel/snooze/query/auth on the fake; idempotent schedule; injected `.failed`/`.uncertain`/`.notAuthorized`/`.unavailable` per operation; uncertain-not-assumed (alarm stays for reconciliation) + uncertain-may-have-applied (orphan via seed); `.restricted` auth; seeded divergence; call-logs vs idempotent set. | Protocol + fake only — the real AlarmKit adapter (imports AlarmKit; persisted `AlarmID`↔system-id mapping; sound/recurrence mapping; coarse-reason redaction) is WG-026. Stop-a-ringing-alarm is a separate primitive at WG-073 (#24). No real-device behavior yet. | Added `AlarmManagerAdapter` (domain-owned port, Foundation-only) + neutral types (`AlarmAuthorizationState` incl. `.restricted`, `AlarmScheduleRequest`, `ScheduledAlarmSnapshot` incl. `isCritical`, `AlarmManagerError` incl. `.uncertain`) in `AlarmDomain`; `FakeAlarmManagerAdapter` (TestSupport; per-op failure/uncertain injection, divergence seed, call logs). Reviews: `alarm-safety-reviewer` + `ios-architect` (SDK-verified, no Blocker). Applied: `.restricted` state, `isCritical` on the reconciliation snapshot (criticality-drift detection), cancel-is-not-stop + cancellation→uncertain docs, non-fabricating snooze, request de-overclaim, + orphan/call-log/restricted tests. WG-025/026/029/073 handoffs in DECISIONS. |
| WG-025 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci` green: 112 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. 9 flow tests: reading the step never prompts (explanation precedes request); each state maps (authorized/denied→Settings/restricted→fallback/notDetermined→explain); interrupted or still-notDetermined request → keep-safe (never assumed denial, #10); Settings opens only when denied; the flow never mutates alarms. | Flow logic only — the permission-center UI (explanation screen + enforcing explanation-before-request + critical-alarm-denial urgency) is a later task; the real UIKit `SettingsOpener` + on-device prompt are WG-026/app-shell. No real-device behavior yet. | Added `AlarmAuthorizationCoordinator` + `AlarmAuthorizationStep` + `SettingsOpener` port (`AlarmApplication`, Foundation-only; read-only w.r.t. alarms) and `FakeSettingsOpener`; extended the adapter fake with `requestAuthorizationCallCount`. Reviews: `alarm-safety-reviewer` + `ios-architect` (SDK-verified, no Blocker). The one MAJOR (explanation-before-request enforceable only by the UI, since the flow can't observe a screen) is resolved by contract + a permission-UI handoff; applied doc corrections + the still-notDetermined test. Handoffs in DECISIONS. |
| WG-026 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci` green: 119 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. 5 mapper tests (one-time future→`.fixed` exact instant, past→nil, weekly today vs tomorrow, identity external-id) + 2 adapter error-mapping tests (#41 coarse redaction — no leaked text; `CancellationError`→`.uncertain`). Mapper + tests compile/run against the real iOS 26.5 AlarmKit SDK; the live adapter compiles (device-only, not unit-tested). | **Per-occurrence `.fixed`, native `.relative` recurrence rejected** after review found 2 BLOCKERs (forks the committed per-occurrence model; cedes tz/DST + can't express fixed-zone → #16 mis-fire) — human-approved pivot. Criticality: AlarmKit has no app-facing criticality knob (SDK-verified); alarms ring through silent/Focus by baseline — **must be device-verified (WG-030)**; the `.critical` tier is policy-layer (WG-028/027). Snooze loses the label + `scheduledAlarms` skips non-`.fixed` + `isCritical` unreadable → WG-027/029 handoffs. Recurrence dependability (re-arm/schedule-ahead) is WG-027/029. | Added `AlarmKitScheduleMapper` (pure; `ScheduleRule`→`.fixed(engine occurrence)` in the caller-resolved zone; identity id correlation) and `SystemAlarmManagerAdapter` (minimal live; only `AlarmManager` caller #1; auth-guard #10; coarse error redaction #41; `.fixed` config) + `WakeGuardAlarmMetadata`. Reviews: `alarm-safety-reviewer` (twice) + `ios-architect` (SDK-verified). Pivot resolved both prior BLOCKERs; applied: cancel-unknown no-op, testable+tested error redaction, honest criticality docs + a real-device checklist item, and the WG-026 ADR. |
| WG-027 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci` green: 129 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. 11 processor tests against real in-memory Core Data repos + fakes: rejection mutates nothing (#3); create schedules + writes outbox + audits (#46); disable cancels + bumps revision; enable schedules; update reschedules with a bumped revision; a stale update is a distinct concurrent-edit failure; `.uncertain` preserves local state + leaves the outbox for reconciliation (#10); `.failed` preserves local state; a cancelled call → `.uncertain`; snooze → `.unsupported` (not a silent success). | Logic vs fakes/in-memory — no real-device behavior (AlarmKit device verification is WG-030). Crash-recovery of a stranded outbox entry / divergence is WG-029 (WG-027 is crash-safe only once WG-029 lands). Strict no-interleave serialization under concurrent commands is a follow-up (actor releases isolation at `await`; the revision guard prevents a lost local update). Occurrence-level commands are `.unsupported` until a follow-on. | Added `AlarmCommandProcessor` (`actor`, single command boundary #2; authorize→persist-local→audit→outbox-bracketed external; `.applied/.rejected/.failed/.uncertain/.noOp/.unsupported`) + the `AlarmPolicyEngine` port + `PolicyDecision` (WG-028 implements it) + `FakeAlarmPolicyEngine`. Reviews: `alarm-safety-reviewer` + `ios-architect` (both SDK-verified, no Blocker). Applied MAJORs: `.unsupported` for intent-discarding commands, stored-entry re-read + retry-cap honoring (no phantom id), `CancellationError`→`.uncertain`, concurrent-edit-vs-storage reason, honest audit reasons. WG-028/029/048/serialization handoffs in DECISIONS. |
| WG-028 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci` green: 141 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. 12 policy tests against real in-memory Core Data repos + fakes: additive/system commands authorized (even a critical from an agent); cancelling a critical needs confirmation (rejected unconfirmed / authorized confirmed, #6); an AI proposal can't suppress a critical even flagged confirmed (#4); an imminent non-critical cancel needs confirmation; a non-imminent one is authorized; `update` gates only critical; an unknown alarm is authorized (no-op); a **storage read error fails closed for both a user and an agent** (never fail-open); a **disabled alarm inside the window is authorized** (not mislabelled "about to go off"); an AI **snooze** of a critical alarm is rejected (#6 "delayed"); cancelling an **occurrence** of a critical alarm needs confirmation; the 300 s window is **inclusive** (300 s gated, 301 s authorized). | Logic vs in-memory Core Data + fakes — no real-device behavior. The engine is **not yet wired into `AppEnvironment`** (only the alarm repo is composed), so #3 is a unit-level guarantee until the UI/processor wiring lands. The confirmation trust boundary (only a user-initiated UI may pass `userConfirmed: true`) is unproven until a real confirming caller exists (WG-044/042/085). | Added `DefaultAlarmPolicyEngine` (`AlarmApplication`, Foundation-only; deterministic authority — criticality/source/confirmation/time-to-fire; user-displayable coarse deny reasons #41); extended the `AlarmPolicyEngine` port with `userConfirmed` (threaded through the processor + `FakeAlarmPolicyEngine`; two conformers/one caller). Reviews: `alarm-safety-reviewer` (**1 BLOCKER** fail-open-on-read-error + **1 MAJOR** `isImminent` ignored `isEnabled` — **both fixed**, probe-proven) + `ios-architect` (**no blocker**; layering/`Sendable`/lint clean; port ripple complete). Applied review fixes: fail **closed** on a thrown read, `isImminent` respects `isEnabled`, + 5 regression tests. The pure-engine design MAJOR is **deferred with rationale** (the sharp edge is fixed; the residual is owned by the WG-027 serialization follow-up). WG-028 ADR + handoffs (WG-044/042/085/171, serialization) in DECISIONS. |
| WG-029 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 156 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. **8 pure-planner tests** (missing→schedule, extra→cancel, divergent fire-time, divergent criticality / silent downgrade #16, matching→empty = idempotent, disabled→extra, multi-repair sorted-by-id, fixed-zone uses anchor zone #16) + **6 driver tests** against real in-memory Core Data + fakes (schedules a missing alarm and audits it `.systemReconciliation`/`.reconciliation`/`.succeeded`; cancels an extra system alarm; idempotent re-run schedules no duplicate; **skips — repairs nothing — when ground truth is unreadable**; **skips and cancels nothing when the desired read throws**, #10; continues after one repair hard-fails; **counts an uncertain repair apart** from a failure, preserving the local alarm). | Reconcile capability + planner only — no real-device behavior. The **launch/foreground trigger + `AlarmCommandProcessor` composition are a follow-on** (nothing invokes `reconcile()` yet — the processor is not composed). Real-device reconciliation verification (missing/extra/divergent, never-cancel-a-ringing-alarm) and the `isCritical` read-back seam are on `RELEASE_CHECKLIST.md`. | Added `AlarmReconciler` (`AlarmDomain`, pure planner — missing/extra/divergent; idempotent) + `ReconciliationSummary` + `AlarmCommandProcessor.reconcile()` (`AlarmApplication`; **ground-truth** reconciliation; #2-preserving same-file extension keeps `alarmManager` private; fail-safe #10 aborts on unreadable ground-truth **or** desired; honest uncertain/cancelled accounting; audits each repair, coarse reasons #41, distinguishable from user edits #50). Moved `CommandOutcome` to its own file to stay under `file_length`. Reviews: `alarm-safety-reviewer` + `ios-architect` (both, **no blocker**). Applied: uncertain-repair accounting + 3 tests. **Deferred with ADR** (liveness/scope, not safety): outbox terminalization/quarantine (**supersedes the WG-027 handoff** — WG-029 does ground-truth reconciliation), the scene-phase trigger + processor composition, the `isCritical` read-back seam, and `SkipReason` observability. WG-029 ADR + the near-limit `AlarmCommandProcessor` decomposition flag in DECISIONS. |
| WG-030 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 167 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. **12 integration tests** (`AlarmSchedulingIntegrationTests`, processor → adapter → real in-memory Core Data): happy schedule + cancel (alarm + outbox `applied` + audit `succeeded` + adapter call); **schedule failure matrix** — notAuthorized / unavailable / failed → `.failed`, local alarm **preserved** (#10), audit records the local save `.succeeded`, no dangling outbox entry; **cancel failure matrix** — uncertain → `.uncertain` + outbox `.uncertain` + local disabled + system still holds the alarm; failed / notAuthorized → `.failed` + local disabled (#10); **cancellation races** on schedule + cancel (a real cancelled `Task` → `.uncertain`, outbox left for reconciliation, local preserved); **occurrence commands** (snooze / cancelOccurrence / rescheduleOccurrence → `.unsupported`, no adapter / outbox / local side effect). | Test-only — **no production code changed**; no real-device behavior. Real-device schedule/cancel/snooze verification is WG-031. **Processor-level snooze remains `.unsupported`** (occurrence-level / WG-073 follow-on — not re-opened here). | Added `AlarmSchedulingIntegrationTests` filling the gaps WG-027's tests left (cancel failure matrix, remaining schedule errors, real cancellation races via `Task.checkCancellation`, occurrence-command safe contract), each asserting the **safe persisted outcome** (#10). Confirmed WG-027's **local/external audit split** (a failed external schedule audits the local save `.succeeded`; the failure lives in the outbox → re-armed by WG-029 ground-truth reconcile). Review: one `alarm-safety-reviewer` pass (no blocker; it re-ran the suite + ~2800 race iterations with 0 flakes). Applied 5 MINOR assertion-tightening findings: terminal-state pins on the schedule/cancel/occurrence paths, an update-reschedule-failure case, and a race-determinism note. WG-030 scope note (snooze not implemented end-to-end) in DECISIONS. |
| WG-031 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | N/A — docs-only (a manual test checklist); no Swift touched, so the automated suite is unaffected (168 tests remain green from WG-030). `make check-tracking` passes. | **This task is the manual checklist:** `docs/DEVICE_SMOKE_TEST.md` — 14 cases (SMK-01–14), each with evidence fields (Result ☐Pass/☐Fail/☐Blocked · Observed · Evidence · Notes), a run-metadata header (device / iOS / build / commit / auth state), and a sign-off block. Executed on a real device per release build (not run here). | Created `docs/DEVICE_SMOKE_TEST.md` covering every acceptance scenario — lock screen (SMK-03), silent/Focus/DND via the **critical-alarm** case (SMK-04), app termination (SMK-08), reboot (SMK-09), repeated/recurring (SMK-10), and actions (SMK-05) — plus authorization (SMK-01/02), cancel/edit (SMK-06/07), reconciliation + the `isCritical` read-back seam (SMK-11/12), and audit/error surfacing (SMK-13/14). Safety-critical cases (SMK-04/08/09 — rings through silent/Focus/DND, after force-quit, after reboot; #9/#10) **block the release**. Low-risk docs task — no reviewer. |
| WG-040 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 173 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. 5 DesignSystem tests (spacing scale ascending / positive / distinct; radii positive; **every status carries text + icon — not color-alone**; statuses distinct by label and icon; critical ≠ scheduled beyond color). The SwiftUI components + dark / light / accessibility-type `#Preview`s compile. | Visual verification is via `#Preview` (dark mode + Dynamic Type) — no automated snapshot (no third-party SDK). On-device contrast of filled controls against the shipped accent → `RELEASE_CHECKLIST.md`. | Added `Sources/DesignSystem/{DesignTokens,AlarmStatusStyle,DesignComponents}.swift` — semantic Dynamic-Type typography, a spacing/radius scale (referenced instead of literals), system-semantic colors (auto dark-mode + Increase Contrast), status = (label, icon, tint) so status is never color-alone; components `StatusBadge` / `SurfaceCard` / `PrimaryButtonStyle` / `DestructiveButtonStyle`; presentation-only (no domain dependency). Review: `ux-accessibility-reviewer` — **1 BLOCKER + 3 MAJOR, all fixed**: StatusBadge drew text/icon in the saturated tint (~2–3:1, failed WCAG AA for every status) → high-contrast label+icon, tint as a background wash only; destructive vs primary was color-alone → destructive now carries a leading danger icon; white-on-fill firmed via a bold control label (+ on-device check); documented the caller a11y-identifier/hint convention. **Deferred with ADR:** non-localizable status `String`s (no string catalog yet — E11 localization). WG-040 ADR in DECISIONS. |
| WG-041 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 186 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. 13 `AlarmListViewModel` tests: empty; **failed ≠ empty** (#10); loaded summary = soonest + sorted; enabled → Scheduled + upcoming; disabled → Off + no-occurrence + excluded from summary; critical → Critical; sort-by-occurrence; **enabled-no-upcoming → Needs attention**; empty-label → "Alarm"; **reload recomputes against an advanced clock**; **critical leads the VoiceOver label**; **disabled label is not "Off, Off"**; reconciling flag toggles. The SwiftUI screen + dark / accessibility-XL / reconciling-banner `#Preview`s compile. | Visual / VoiceOver / Dynamic-Type verified via `#Preview` + on device (`RELEASE_CHECKLIST.md`): next-ring accuracy after an alarm fires (foreground reload), row reflow at accessibility sizes, and critical-alarm prominence. | Added `AlarmListViewModel` (`@MainActor @Observable`, read-only #2; states loading / empty / loaded(summary + list) / failed, plus a **non-blocking `isReconciling` banner**) + `AlarmListView` (a `List` with a Next-alarm summary section + rows using `StatusBadge`; nil-environment safe state; combined VoiceOver labels; `ViewThatFits` reflow); `RootView` now hosts it. This is the **E03 screen template** (VM owned by the view via `@State`; ports from `@Environment(\.appEnvironment)`; next occurrence via the **same** engine call as the processor/reconciler). Reviews: `ux-accessibility-reviewer` + `ios-architect` — **no blocker; 5 MAJOR applied**: foreground reload + latest-load-wins guard (stale next-ring), relative next-ring text (was an absolute datetime), VoiceOver fixes (no "Off, Off"; critical announced first), Dynamic-Type reflow, reconciling → non-blocking banner. **Deferred with ADR:** reconcile trigger (WG-029), time-zone-change reload, a feature-UI folder, localization (E11), extra critical *visual* prominence (WG-044). WG-041 ADR in DECISIONS. |
| WG-042 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 197 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. 11 `CreateAlarmViewModel` tests: weekly + one-time build & submit `.create` with the **exact** schedule fields (time / zone / date); preview-as-validation (empty weekdays / past one-time / **today-earlier-time** / live toggling → can't save); **a lapsed one-time is re-validated at save and refused** (no dead alarm submitted); deferred `.uncertain` → created + `.failed` → failed(reason); an empty label stores `""`. (6 `AppEnvironment` tests unaffected by the new field.) | Real AlarmKit scheduling is **deferred** — the interim `DeferredAlarmManagerAdapter` makes no AlarmKit calls, so alarms are **saved but do not ring yet**, disclosed by a persistent banner. On-device create + the AlarmKit authorization flow + the real-adapter swap are WG-025-UI / WG-030 (`RELEASE_CHECKLIST.md`). Visual / VoiceOver via `#Preview`. | Added `CreateAlarmViewModel` + `CreateAlarmView` (form → validated `Alarm`; `canSave = nextOccurrence != nil`, re-checked at save); submits via a new `AlarmCommandProcessing` boundary (the UI depends on the protocol, not the actor; only the processor invokes the adapter #2/#3/#46). **Composed `AlarmCommandProcessor` + `DefaultAlarmPolicyEngine` into `AppEnvironment`** with the interim adapter (`schedulesAlarmsInSystem == false`). Reviews: `alarm-safety-reviewer` + `ios-architect` + `ux-accessibility-reviewer` — **no blocker**. Applied: `save()` future-ness re-check (top safety finding), the "won't ring yet" disclosure banner, a11y (chips reflow via `LazyVGrid` + `.isToggle` + non-color selected cue + 44pt; live preview `updatesFrequently`; Save hint; labelled field), adapter `snooze → .unavailable`, +4 tests. **Deferred with ADR:** the real-adapter outcome-mapping seam (a genuine schedule `.failed` with the alarm saved would wrongly read "couldn't create"), the AlarmKit integration, criticality toggle (WG-044), DST-boundary create (WG-022 + checklist). WG-042 ADR in DECISIONS. |
| WG-043 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 212 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. 11 `AlarmEditFlowTests` (enable/disable/delete route through the processor; a critical destructive action prompts then re-submits confirmed; cancelling leaves it unchanged; a hard failure surfaces a reason + leaves a safe state; **a non-confirmable `.rejected` surfaces as an error, never a prompt**; edit submits `.update` with same id / bumped revision / preserved fields; editing a critical alarm needs confirmation; edit pre-fills) + processor **`testDeleteWithFailedSystemCancelStillReportsDeleted`** (a failed system cancel doesn't undo a done local delete → `.applied`, local gone, audited `.succeeded`) alongside the existing delete / critical-delete tests; the 6 policy confirmation cases re-pinned to the new `.needsConfirmation`. | Real-device edit / enable-disable toggle / **no-full-swipe** delete + the critical-alarm confirmation prompt (VoiceOver announces the alarm + on/off consequence; Dynamic Type reflow) → `DEVICE_SMOKE_TEST.md` (SMK-15) / `RELEASE_CHECKLIST.md`. Alarms are still **saved-not-ringing** (interim adapter, WG-042). Visual/VoiceOver via `#Preview`. | Added `.delete` (domain + policy `isDestructive` + processor `applyDelete`): **local-delete-first then a best-effort system cancel** — a failed cancel does **not** downgrade the delete (returns `.applied`; a stranded system alarm is the safe direction — a spurious ring, never a missed one — reaped by WG-029 #10); cancel-first is rejected (a cancel before a failed local delete would silently stop a still-listed alarm ringing). Row actions (tap-edit / enable-disable toggle / no-full-swipe delete) + a unified confirm/error alert; `CreateAlarmView` doubles as the edit form (`.update`, same id, revision+1, preserved criticality/travel/sound/policies). Introduced a first-class **`.needsConfirmation`** outcome (`PolicyDecision` + `CommandOutcome`) so a confirmable #6 gate is distinct from a non-confirmable fail-closed `.rejected`; the processor authorizes via an exhaustive switch so a gated command can never apply. Extracted `AlarmCommandProcessor+Reasons.swift` + `AlarmListComponents.swift` to hold both files under `file_length`. Reviews: `alarm-safety-reviewer` + `ios-architect` + `ux-accessibility-reviewer` — **no blocker**. Applied: delete-outcome coherence + honest comment (safety B1/M1); the `.needsConfirmation` split (arch M1); no-full-swipe + pending-path reload + confirm-binding-restore-on-dismiss + toggle a11y (UX B1/M1/M2/m4). **Deferred (DECISIONS handoffs):** full `AlarmCommandProcessor` decomposition; telegraph critical status in the edit form (WG-044); row-action in-flight serialization; delete-outbox distinct key; imminent non-critical *edit* gating (WG-028 choice); past-one-time-edit UX. WG-043 ADR in DECISIONS. |
| WG-044 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 219 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. Policy tests: a **user** may create a critical alarm (authorized); a **model** cannot raise criticality on update (rejected #31); a user **weakening** critical→standard needs confirmation (#6, then authorized confirmed); the **#31 guard precedes the additive fast-path** (ordering pin). Rewrote the WG-028 agent-additive test to assert an agent may add a *standard* alarm / enable one but **cannot create a critical alarm** (#31). VM tests: a new alarm defaults standard; the toggle submits a critical alarm; editing a critical alarm **seeds the toggle on** and can weaken it. | On-device: the "Critical alarm" toggle + plain-language explanation legible and reflows at the largest Dynamic Type; VoiceOver reads the toggle + hint; **rings-through-silent/Focus/DND is device-verified via SMK-04** once AlarmKit scheduling is wired — the interim adapter doesn't ring yet, so the copy says "designed to ring" → `DEVICE_SMOKE_TEST.md` / `RELEASE_CHECKLIST.md`. | Added a `Critical alarm` toggle to the create/edit form (`CreateAlarmViewModel.isCritical`, seeded from the edited alarm, threaded into both build paths — the edit path now takes criticality from the toggle, closing the WG-043 M3 gap). Added a policy **#31 guard** (`modelCriticalityChange`): an `.agentProposal` command that creates a critical alarm or changes an alarm's criticality is **rejected**, placed **before** the additive fast-path so an agent `.create(critical)` can't slip through; fails safe (an unreadable/absent update target reads as `.standard`, so any non-standard incoming criticality is rejected). Weakening a critical alarm (user) still needs confirmation via the WG-028/043 gate (which reads the *stored* criticality); strengthening / user-create-critical are additive → authorized. Reviews: `alarm-safety-reviewer` (**no blocker/major; #31 verified complete + fail-safe, a net strengthening**) + `ux-accessibility-reviewer` (**no blocker**). Applied: softened the ring-through-silent copy to "designed to" (activates once WG-025/031 wire scheduling) + a guard-ordering regression test. **Deferred (DECISIONS):** gating the copy on `schedulesAlarmsInSystem`; a stronger VoiceOver footer binding (iOS-idiomatic; the consequence is also spoken at the Save confirmation); localization (E11). WG-044 ADR in DECISIONS. |
| WG-045 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 231 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. `ChallengeConfigTests` (13): the `.standard` factory fills validated anti-cheat defaults; `ChallengeDraft` builds none/walk and seeds/round-trips; **out-of-range input is clamped within the domain's validated bounds**; a walk always carries an accessible fallback (SCOPE §2.3); and — the safety fix — **`testConfiguredWalkCadenceStaysInThePlausibleBand`** (grid sweep) + **`testDegenerateCornersAreClampedToSafeChallenges`** prove no reachable config is unsatisfiable (required cadence ≥ the 4.0/s anti-cheat cap → lockout) or trivially defeatable; plus VM integration (create threads the challenge, default is none, edit seeds it). | On-device: the "Wake challenge" section (None/Walk, bounded duration/steps steppers, accessible-alternative picker) is legible + reflows at the largest Dynamic Type; VoiceOver reads each control's label + value; the phone-carry disclosure + "always available" alternative are clear. The **actual walk detection / pass-fail is WG-072/075** (not exercised here) → `RELEASE_CHECKLIST.md`. | Added a `ChallengeSection` to the create/edit form + `ChallengeDraft` (form state → validated `ChallengePolicy`, seeded from the edited alarm, threaded into both build paths). Only the safe facets are user-facing (duration, minimum steps, accessible alternative); the anti-cheat internals use a new domain `WalkChallenge.standard` factory's validated defaults (cadence cap 4.0/s, pauses 2, threshold 0.5) and are **not exposed**, so the UI can't weaken them. Duration and steps are **coupled** so the required cadence (steps ÷ duration) stays in a plausible-walk band — ≤ 2.0/s (below the anti-cheat cap → always satisfiable, no false-fail/lockout) and ≥ 0.5/s (no trivial false-pass). Reviews: `ux-accessibility-reviewer` (**no blocker**) + `motion-red-team` (**no blocker**; found the decoupled-bounds corners — now closed by the coupling). Applied: the cadence coupling (motion M1/M2) + stable Stepper VoiceOver label/value (ux M1). **Deferred (DECISIONS):** a domain-level satisfiability invariant (WG-072/075 owns real motion units); the section-footer VoiceOver trade-off (iOS-idiomatic; parity with WG-044); `antiCheatThreshold` semantics (WG-075); localization (E11). WG-045 ADR in DECISIONS. |
| WG-046 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 240 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. `TravelPolicyTests` (9): the 3 options round-trip to/from `TravelBehavior` and a **region rule maps to Ask**; each option has a distinct title + plain-language preview; the create/edit VM threads the choice (default follow-local) and seeds it on edit; the anchor is the device zone on create; **`testEditingInAnotherZonePreservesTheStoredAnchor`** (edit a NY alarm while the device is in Tokyo → the saved anchor stays `America/New_York`, #16); and **`testPreviewCopyNeverImpliesLocationTracking`** (no option's copy contains GPS/location/track; Ask keeps its "never moves the alarm on its own" assurance). | On-device: the "When you travel" section (3-option picker, accessible anchor-zone display, per-option destination preview) is legible + reflows at the largest Dynamic Type; VoiceOver reads the picker + anchor value ("America, New York"). The runtime travel **detection** is E06 (not exercised here) → `RELEASE_CHECKLIST.md`. | Added a `TravelSection` to the create/edit form + `TravelOption` (the 3 MVP options ↔ domain `TravelBehavior`; `.regionRule` → Ask). The VM threads `travel` into both build paths, seeds from the edited alarm, and exposes `anchorZoneID`. **Fixed a pre-existing WG-044 re-anchor defect (#16, the review's one MAJOR):** `buildSchedule` now **preserves an edited alarm's stored anchor zone** instead of silently re-anchoring to the current device zone, and `anchorZoneID` displays the stored anchor — so editing a "keep home-zone" alarm while travelling no longer moves its zone (the picker time still decomposes in the device zone, so the number round-trips; the stored `IANATimeZone` is reused directly to avoid the UTC→GMT normalization). Copy honesty: `.ask` is stated as the alarm's *policy* (detection is E06), and `.followLocal` avoids "wherever you are" so nothing implies location tracking. Reviews: `ux-accessibility-reviewer` (**no blocker**; MAJOR anchor-preservation applied) + `privacy-security-reviewer` (**no blocker**; no Core Location, IANA not offsets #11, data-minimized). **Deferred (DECISIONS):** a friendlier localized zone name (the IANA identifier is what the acceptance shows); the section-footer VoiceOver trade-off (parity with WG-044/045); editing a fixed-zone alarm's *time* across zones (wall-clock nuance, WG-022); localization (E11). WG-046 ADR in DECISIONS. |
| WG-047 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 254 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. `PreAlarmPolicyTests` (14): the domain carries the spec's allowed actions and **still decodes pre-WG-047 data** (missing key → empty set); zero lead time still throws; actions round-trip through Codable; `PreAlarmDraft` builds/seeds the window + actions, **clamps an out-of-range lead time within bounds** and **aligns an off-grid seed to the 5-minute grid**; **`promptDisclosure` states #7 ("unchanged"), the informational-only note when no actions are offered, and the critical-alarm confirmation limit**; VM threads the config (default disabled), seeds on edit. | On-device: the "Smart pre-alarm" section (enable toggle, lead-time stepper, three action toggles, disclosure footer) is legible + reflows at the largest Dynamic Type; VoiceOver reads the toggle/stepper (label + value) and each action toggle (+ hint); the #7 copy and the critical limit are visible. The pre-alarm prompt **runtime is E05** (not exercised here) → `RELEASE_CHECKLIST.md`. | Added a "Smart pre-alarm" section + `PreAlarmDraft` (form → validated `PreAlarmPolicy`, bounded 5–120 min window, allowed-actions set; seeded from the edited alarm; threaded into both build paths). **Minimally extended the domain `PreAlarmPolicy` with `allowedActions: Set<PreAlarmAction>`** (the spec's Turn off today / Change time / Remind later; "Keep original" is the implicit no-op) — defaulted factory param + backward-compatible decode + unknown-action fail-closed (#27). The set records **intent only**: the pre-alarm prompt runtime is E05, and any destructive action it triggers still routes through the policy engine, which gates a critical alarm behind #6 (proven by `testUpdateGatesOnlyCriticalAlarms`). `promptDisclosure` always states #7 and appends the critical confirmation limit live (via `model.isCritical`). Reviews: `alarm-safety-reviewer` (**no blocker/major**; #6/#7 enforced by tested code, decode fails closed, no config bypasses the engine) + `ux-accessibility-reviewer` (**no blocker**). Applied: the informational-only note for enabled-with-no-actions (ux M1), a 5-minute-grid seed alignment (safety MINOR-2), action-toggle VoiceOver hints (ux m1), de-duplicated header vs toggle label (ux m4). **Deferred (DECISIONS):** the critical limit stays in the footer (parity with WG-044); per-toggle critical annotation (runtime enforces #6); the pre-alarm evaluation/awake-inference runtime (ADR-008 / E05); localization (E11). WG-047 ADR in DECISIONS. |
| WG-048 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 260 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. `AlarmHistoryViewModelTests` (6): empty vs **failed-not-empty** on a storage throw; newest-first ordering; who/what/outcome are human-readable; **recovery AND reconciliation are distinguishable from ordinary edits** (#50); and **`testSensitiveInternalsAreNotExposed`** injects a state hash + a correlation UUID and asserts neither appears in any surfaced string (#41). | On-device: the per-alarm "History" (opened from the edit form) lists who/what/when with an outcome tag; a system/recovery row is marked by icon + a "System" tag (not color-alone) and reads "System action…" to VoiceOver; a detail shows What/Who/When/From/Result; a failed load reads distinctly from empty → `RELEASE_CHECKLIST.md`. | Added `AlarmHistoryViewModel` (maps each `AuditEvent` → a safe `AlarmHistoryItem`: who/what[=`userVisibleReason`]/when/outcome/source/isSystem — the state hashes + correlation id are **structurally absent**, so they can't reach the UI, #41) + `AlarmHistoryView` (read-only List + per-event detail, reusing the WG-041 loading/empty/failed pattern; nil-environment safe state) + a "History" link on the edit form. **System-originated actions (reconciliation / recovery / migration, by actor or source) are distinct** via a distinct icon + "System" tag + VoiceOver prefix (#50) — broadening the domain's narrower `isRecovery` so reconciliation is distinguished too. Reviews: `privacy-security-reviewer` (**PASS, no findings** — traced the full `AuditEvent`→UI flow, confirmed hashes/correlationID/raw command/label never surface, read-only, no logging) + `ux-accessibility-reviewer` (**no blocker**). Applied ux: the system-distinction + detail restructure (drop the redundant "From" for system rows) (M1); the outcome/tag line **reflows at large Dynamic Type** so the non-color outcome can't truncate (M2); `noOp`→"No change needed"; plainer "Automatic recovery". **Deferred (DECISIONS):** a top-level all-history view + deep links (WG-049); the interpolated a11y label's English word-order + localization (E11). WG-048 ADR in DECISIONS. |
| WG-049 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 273 tests pass (0 failures), 0 warnings, swiftlint 0, swift-format clean, tracking OK. `DeepLinkTests` (13): the parser is **total** — valid alarm/proposal links parse; wrong scheme / unknown host / invalid-UUID / missing-id / extra-segments / (case-insensitive) all → `.unknown`, no crash; the resolver **presents an alarm without mutating it** (stored revision unchanged, #7/#3), a **stale id → safe error** (#2), a thrown read → "try again" (distinguished), a proposal → "not available" (no E09 screen invented), nil-environment → safe; and a **newer link supersedes/clears a prior result**. | **On-device delivery is a documented follow-up (see below):** the routing *logic* is complete + tested, but the `wakeguard://` URL scheme is not yet registered, so `onOpenURL` doesn't fire on device yet — fail-closed (nothing emits these URLs until the notification/AlarmKit work, E05/WG-025-030). Device verification of criterion 1 (notification/AlarmKit routing) is pending that registration → `RELEASE_CHECKLIST.md`. | Added `DeepLinkRoute` (`.alarm(AlarmID)` / `.proposal(UUID)` / `.unknown`) + `DeepLinkParser` (pure, total; a route **structurally carries no `AlarmCommand`**, so a link can only navigate, never mutate — #7) + `DeepLinkModel` (reads the alarm repo to present the edit screen or a safe error; a request token supersedes an in-flight link) + `RootView` `.onOpenURL` wiring. Reviews: `alarm-safety-reviewer` (**no blocker/major** — fuzzed ~30 hostile URLs, all → exact target or `.unknown`; verified empirically that opening a link never calls the command processor and a critical edit still routes through the #6 gate on Save) + `ios-architect`. Applied the rapid-link race fix (request token + reset-both-fields, tested). **Deferred with ADR (coupled, land with the URL emitters E05/WG-025-030):** (a) the `CFBundleURLTypes` scheme registration (needs an XcodeGen `info:` block replacing `GENERATE_INFOPLIST_FILE` — an un-device-verifiable Info.plist change; fail-closed until done; recipe recorded); (b) consolidating the deep-link edit sheet with the list's single presenter (a link arriving while a sheet is open currently no-ops — never mutates); (c) E05 must keep notification actions navigational-URL-only. WG-049 ADR in DECISIONS. |
| WG-050 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | **Unit gate** `make ci-fast` green: 273 tests, 0 warnings, swiftlint 0 (109 files, incl. the UI-test code), swift-format clean, tracking OK. **`make test-ui` green: 6/6 XCUITest flows pass** on iPhone 17 / iOS 26.5 (88–90 s): launch→empty-list, **create** (row appears), **edit** (round-trips a critical change through persistence — reopen shows it on), **delete** (row removed), **critical** (swipe-delete → the #6 "Confirm change" alert appears; Cancel leaves it), **travel** (pick "Keep home-zone time" → saves). Screenshots of key states attached (`XCTAttachment`, `keepAlways`). | Screenshot baseline **approval** on device + running the UI suite in remote CI are release items → `RELEASE_CHECKLIST.md`. | Added a `WakeGuardUITests` (`bundle.ui-testing`) target + `UITests/CoreAlarmFlowsUITests.swift`, driven entirely through the app's accessibility identifiers (stable ids added across WG-041–049). The app honors a **`-uiTesting` launch argument → `AppEnvironment.inMemory()`** so each launch starts from a clean, empty, isolated store and never touches the user's real alarms — **gated behind `#if DEBUG`** so the affordance is compiled out of release builds. Kept in its own scheme (`make test-ui`), **not** the `WakeGuard` scheme's test set, so `ci-fast` stays a fast unit-only gate. XCUITest robustness recorded: the row is matched by identifier on any element type (its children are combined into one a11y element); a SwiftUI `Toggle` needs a verify + coordinate-tap fallback; the critical-create flow leaves the label blank so the keyboard can't cover the toggle. **No reviewer** (low-risk test infrastructure; only the DEBUG-gated launch hook is production). WG-050 ADR in DECISIONS. |
| WG-060 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 281 tests pass (0 failures), 0 warnings, swiftlint 0 (incl. `domain_no_apple_frameworks` — `MotionDomain` is Foundation-only, no CoreMotion), swift-format clean, tracking OK. `MotionSourceTests` (8): every sample exposes timestamp + quality; an available source yields its samples in order; an **unavailable source reports the state AND its stream throws** (never a silent empty, #21); the **four ports are independently usable**; unknown enum values **fail to decode** (#27); samples round-trip Codable; a **source dropping out mid-attempt throws after delivering some** (the false-FAIL trace, #24); and the anti-cheat signals survive Codable. | None for the ports themselves; on-device motion behaviour is verified with the walk-challenge runtime → **UAT CP-C** / `RELEASE_CHECKLIST.md`. | Added `Sources/MotionDomain/MotionSource.swift`: four **independent** Sendable ports (`PedometerSource`/`MotionActivitySource`/`DeviceMotionSource`/`AltimeterSource`), each `availability() async` + a live `samples()` `AsyncThrowingStream`; normalized samples (`PedometerSample`/`MotionActivitySample`/`DeviceMotionSample`/`AltitudeSample`) each carrying **timestamp + quality** via a `MotionSample` protocol; an **explicit** `MotionSourceAvailability` (notPresent / notAuthorized / restricted / temporarilyUnavailable) + `MotionSourceError.unavailable`; TestSupport fakes (with a mid-stream-drop injector). Reviews: `ios-architect` + `motion-red-team`. Addressed the false-PASS **blocker** by freezing the discriminating signals into the Codable contract now — `PedometerSample.secondsSinceLastStep` (cadence *regularity* series, not just a smoothed scalar) and `DeviceMotionSample.gravityAngleFromVerticalRadians` (a privacy-safe scalar separating carried-vs-shaken) — plus documented the throw-is-terminal / non-replayable / non-monotonic stream contract and made the mid-attempt drop testable. **Deferred with ADR:** sample-value validation lives at the `MotionInfrastructure` adapter boundary (WG-062) + on decode when a persisted motion-history store lands (WG-081); the historical bounded-window query is a separate additive port (WG-062/081). WG-060 ADR in DECISIONS. |
| WG-061 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 290 tests pass (0 failures), 0 warnings, swiftlint 0 (`MotionDomain` Foundation-only — no CoreMotion), swift-format clean, tracking OK. `MotionAuthorizationFlowTests` (9): each status routes correctly (notDetermined→explainThenRequest, authorized→useWalkChallenge, denied→alternative+Settings, restricted→alternative); **reading the step never prompts** (in context); a grant→walk (request made once); **denied / restricted / interrupted / still-not-determined-after-request all route to the accessible alternative** (never trapped, #21, no re-prompt loop); the purpose copy is specific + honest; an unknown status **fails to decode** (#27). | On device: Motion & Fitness is requested **in context** after the specific explanation; declining still lets the user turn off the alarm via the accessible challenge; purpose copy reads correctly → **UAT CP-C** / `RELEASE_CHECKLIST.md`. | Added `Sources/MotionDomain/MotionAuthorization.swift`: a pure `MotionChallengeAuthorizationCoordinator` over a domain-owned `MotionAuthorizing` port, returning a closed `MotionChallengeAuthorizationStep`; mirrors WG-025's AlarmKit flow (reads/requests the shared grant, decides the next step; touches no alarms/samples/persistence). **Never-trap safety core:** every non-authorized outcome routes to the accessible alternative — a denial/interruption can only *route*, never silently weaken/skip the challenge. `MotionChallengePurpose.explanation` is specific (steps/movement for the wake walk), disclaims location **and** saved workouts/health records (#41), and discloses the alternative. Reviews: `motion-red-team` + `ios-architect` (**no blocker** — never-trap verified, layering/concurrency/fail-closed confirmed). Applied: documented that `useWalkChallenge` ≠ hardware present (the E04 runtime re-checks per-source `availability()` for `.notPresent`); tightened the copy honesty; + restricted/no-loop/decode tests. **Deferred (handoffs):** Settings-opening is decide-only (hoist a shared `SettingsOpener` when the real one lands — permission-center); an explicit interrupted-retry affordance (the UI); localization (E11). WG-061 ADR in DECISIONS. |
| WG-062 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 303 tests pass (0 failures), 0 warnings, swiftlint 0 (`MotionDomain` Foundation-only — no CoreMotion; CoreMotion lives only in `MotionInfrastructure`), swift-format clean, tracking OK. `HistoricalPedometerTests` (12): a valid window constructs; the window **rejects** start≥end / future end / overlong span / **non-finite dates** (NaN clock can't slip past the future guard — review B1); the sample validation **accepts** plausible values and **rejects** negative steps / non-finite-or-negative measures / **non-finite timestamp** / **a timestamp outside the queried window** (review S1); availability maps hardware + auth (absent hardware dominates a grant; notDetermined→notAuthorized so a not-yet-asked grant can't fabricate "no steps"); CoreMotion's status maps to the domain; and an **unavailable source throws, never returns empty** (#21/#24). | On device (**UAT CP-C**): a real `CMPedometer` history query over a bounded window returns a validated sample; denied / no-pedometer-hardware **throws** and the walk challenge falls back to the accessible alternative; no raw sample or CoreMotion error text appears in logs (#41) → `RELEASE_CHECKLIST.md`. | Added `Sources/MotionDomain/HistoricalPedometer.swift` (domain: `HistoricalPedometerSource` port + `PedometerQueryWindow` + `PedometerSample.validated` + `MotionSourceAvailability.forPedometer`), `Sources/MotionInfrastructure/CoreMotionHistoricalPedometerAdapter.swift` (the `actor` over `CMPedometer`), and `Tests/TestSupport/FakeHistoricalPedometerSource.swift`. A **separate bounded-window port** from WG-060's live stream; **WG-060's deferred boundary validation lands here** (finite/ordered/non-future/bounded window; finite in-window timestamp + non-negative steps + finite/non-negative measures). **#41:** raw `CMPedometer` error discarded; nil-data *and* corrupt-reading both map to the single coarse `MotionSourceError.unavailable(.temporarilyUnavailable)`, so no foreign `MotionQueryError` and no value-naming reason string escapes the adapter. Reviews: `privacy-security` (**clean**) + `motion-red-team` + `ios-architect` (continuation resume-once + no callback-thread race confirmed). Applied: **B1** NaN-`now` finiteness guard, **S1** timestamp-in-window check, the **fail-open** remap, and a `Task.checkCancellation()` fast-fail. **Deferred (with rationale):** quality stays `.high` (CMPedometer history is authoritative; the E05 consumer weights by recency/window span); no absurd-but-finite plausibility caps (that's the live-stream regularity job — WG-063/069/070); the in-flight one-shot query is uninterruptible (no `stop…` counterpart; caller timeout still fails closed). WG-062 ADR in DECISIONS. |
| WG-063 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 311 tests pass (0 failures), 0 warnings, swiftlint 0 (`MotionDomain` Foundation-only; CoreMotion + `Synchronization` only in `MotionInfrastructure`), swift-format clean, tracking OK. `LivePedometerTests` (8): the normalizer **drops** duplicate / out-of-order / invalid (negative-steps, non-finite timestamp) / **implausibly-future (and does not poison the watermark)** / **cumulative-count regression**, and emits an equal-count stationary reading; the adapter driven through the fake seam **emits only ordered valid samples from a recorded trace** (`[3,6,9]` from a dup/out-of-order/invalid-laced stream), **stops updates on cancellation** and on a terminal stream error, and an **unavailable source throws without ever starting** CoreMotion (never an empty completion, #21/#24). | On device (**UAT CP-C**): a real walk yields a live step stream that reaches the challenge; cancelling the challenge **stops** `CMPedometer` updates (no overnight battery/privacy leak); denied/no-hardware throws → accessible alternative; no raw sample or CoreMotion error text in logs (#41) → `RELEASE_CHECKLIST.md`. | Added `Sources/MotionDomain/LivePedometer.swift` (`PedometerReading`, the `LivePedometerUpdates` seam port, and the pure `LivePedometerNormalizer`), `Sources/MotionInfrastructure/CoreMotionLivePedometerAdapter.swift` (the `PedometerSource` adapter + `CMPedometerLiveUpdates` seam), and `Tests/TestSupport/FakeLivePedometerUpdates.swift`. **CoreMotion is isolated behind the injectable seam** so streaming + cancellation are tested off-device; the normalizer makes the port's explicitly non-monotonic stream ordered + well-formed (advancing its watermark only on an emitted sample). **Cancellation-safe:** `onTermination`→`stop()` set *before* start (exactly-once stop, no leak); the unavailable path starts/stops nothing; an error `finish(throwing:)`s rather than completing empty (fail-closed). Concurrency: `Mutex<LivePedometerNormalizer>` for synchronous ordered mutation from the sync `@Sendable` CoreMotion callback (an actor would reorder). **#41:** raw error discarded → coarse `.temporarilyUnavailable`; invalid dropped via `try?`. Reviews: `ios-architect` (concurrency/cancellation **sound** — stop exactly-once, watermark atomic) + `motion-red-team` + `privacy-security` (**clean**). Applied: **B1** future-timestamp guard (watermark-poison + false-freshness), **S1** cumulative-regression drop, **S2** comment honesty (the *consumer* owns the fail-closed timeout), and a **serial-queue** hardening of the seam's `@unchecked Sendable`. **Deferred (documented):** uniform `.high` quality (regularity is WG-069/070's axis, not a per-sample scalar); `secondsSinceLastStep` is unavailable from CMPedometer live data → WG-069/070 reconstruct it. WG-063 ADR in DECISIONS. |
| WG-064 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 322 tests pass (0 failures), 0 warnings, swiftlint 0 (`MotionDomain` Foundation-only; CoreMotion only in `MotionInfrastructure`), swift-format clean, tracking OK. `MotionActivityTests` (11): a **single flag maps** to its kind (walking/stationary/running/automotive); an explicit `unknown`, no flag, or **any multi-flag ambiguity resolves to `.unknown`** (conservative — never a false confident class); `sample()` **retains confidence + timestamp** and **drops a non-finite timestamp**; confidence maps 1:1 to quality; **availability degrades safely** (unsupported→notPresent, denied/notDetermined→notAuthorized, restricted→restricted); the adapter via the fake seam emits mapped samples + **drops nil/invalid**, a **low-confidence `.walking` survives un-dropped** (no confidence gating), **stops updates on cancellation**, a **post-cancellation reading is a safe no-op**, and an **unavailable source throws without starting** (never an empty "no activity = asleep" stream, #21/#24). | On device (**UAT CP-C**): a real walk classifies as walking and reaches the challenge; an unsupported/denied device throws → accessible alternative; no raw activity or CoreMotion state in logs (#41) → `RELEASE_CHECKLIST.md`. | Added `Sources/MotionDomain/MotionActivity.swift` (`MotionActivityReading`, the `MotionActivityUpdates` seam port, the conservative `resolvedKind`/`sample()` resolution, and `MotionSourceAvailability.forMotionActivity`), `Sources/MotionInfrastructure/CoreMotionActivityAdapter.swift` (the `MotionActivitySource` adapter + `CMMotionActivityUpdates` seam with confidence mapping), and `Tests/TestSupport/FakeMotionActivityUpdates.swift`. **Conservative resolution** (ambiguity→`.unknown`) fails safe for the anti-cheat; confidence passed through **un-gated** (WG-068/069 own the threshold); timestamp retained. **Stateless** (activity not cumulative) → no watermark, no `Mutex`, and **no freshness guard here** — the port assigns stale/out-of-order rejection to the consumer (**handoff: WG-067 must add the future/stale-timestamp rejection test**, the WG-063 B1 analog). Concurrency: two-queue seam (serial `controlQueue` orders start/stop; concurrent `OperationQueue` delivery harmless because the map is stateless + `yield` is multi-producer-safe). #41: no raw activity/error logged; auth reuses `CoreMotionHistoricalPedometerAdapter.map`. Reviews: `motion-red-team` (**no blocker** — conservative resolution + fail-closed confirmed) + `ios-architect` (**sound** — stateless race-free, two-queue `@unchecked` justified); privacy a strict subset of WG-063 (clean), not re-run. Applied two hardening tests (low-confidence walking un-gated; post-cancel no-op) — no production change. WG-064 ADR in DECISIONS. |
| WG-065 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 336 tests pass (0 failures), 0 warnings, swiftlint 0 (`MotionDomain` Foundation-only), swift-format clean, tracking OK. `DeviceMotionEvidenceTests` (14): a still window → `.stationary`; a settled gentle reorientation → `.pickup`; forceful-without-reorientation and erratic-wide-wobble → `.irregularShaking`; and every non-classifiable case → `.insufficient` — a **forceful reorientation** (violent grab), **ambiguous** moderate motion, a **sub-0.8g narrow-wobble**, **too few** samples, **missing orientation** (can't separate carry from shake), a **non-finite** magnitude, and the **empty** window (nil bounds). A **single glitched endpoint angle does not manufacture a pickup** (median fix), a **slow deliberate tilt is `.pickup` by design** (documented limitation), and the evidence **retains window bounds + count**. | On device (**UAT CP-C** + performance): measure the **CMDeviceMotion battery cost** at the chosen sampling rate/window (the acceptance's device-only criterion) and **calibrate the thresholds** on real still/pickup/shake motion → `RELEASE_CHECKLIST.md`. | Added `Sources/MotionDomain/DeviceMotionEvidence.swift`: `DeviceMotionCharacter` (String-raw, #27), `DeviceMotionEvidence` (character + count + window, **no distance/position** — cannot claim displacement by construction), and the pure conservative `DeviceMotionEvidenceAnalyzer.evaluate` (+ tunable `Thresholds`). Discriminators: max acceleration/rotation, gravity-angle spread (erratic) and **net reorientation via a head/tail median** (settled). Order stationary→shaking→pickup→insufficient so a shake/ambiguity is **never credited as a carry**; **no acceleration is integrated into distance** (honesty invariant). Supporting-only, total function (never traps, #21). Review: `motion-red-team` (**no blocker** — shake≠pickup + no-displacement confirmed); privacy/architecture not run (pure Foundation, no I/O/logging/state). Applied: **A2a** head/tail-median net reorientation (a glitch endpoint can't fake a pickup) + **A1** documented the slow-tilt=pickup limitation (consumers must not read `pickup` as a walk — WG-069 is the gate) with a pinning test + sub-threshold-shake regression pin. **Deferred:** battery-cost measurement + on-device threshold calibration → device checklist. WG-065 ADR in DECISIONS. |
| WG-066 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 348 tests pass (0 failures), 0 warnings, swiftlint 0 (`MotionDomain` Foundation-only), swift-format clean, tracking OK. `AltitudeEvidenceTests` (12): a **fast rise** and a **fast descent** → `.significantChange` (direction-agnostic); a **slow drift that reaches 0.8 m over 60 s** stays `.flat` (rate discriminator — pressure drift does not pass), as do a **small drift**, **no change**, and a **single altitude spike** (median endpoints reject it); an **unavailable/notAuthorized/restricted/temporarilyUnavailable** barometer → `.unavailable` and **never corroborates** (no negative impact); **too few / non-finite / zero-duration / empty** → `.insufficient`; and the **supporting-only invariant** holds (only `.significantChange` corroborates; no failure case). | On device (**UAT CP-C** + performance): confirm a real stand-up/stairs corroborates while sitting still / weather drift does not; **calibrate thresholds** and confirm the barometer's battery cost is acceptable (bounded window) → `RELEASE_CHECKLIST.md`. | Added `Sources/MotionDomain/AltitudeEvidence.swift`: `AltitudeEvidence` (String-raw, #27; `corroboratesMovement` — exactly one positive case, **no failure case** so it's strictly additive) + the pure `AltitudeEvidenceAnalyzer.evaluate(_:availability:)` (+ `Thresholds`). **Supporting-only** enforced by the type; **unavailable→neutral**; drift rejected by requiring **both** magnitude and rate (slow drift clears at most one). **Honest caveat:** a fast *step* transient (door/HVAC/elevator) can clear both — barometry can't tell it from a stand-up, so it's weak evidence and **WG-067/069 must not pass on a lone `.significantChange`** (documented handoff). Robust head/tail-median endpoints, clamped disjoint. Review: `motion-red-team` (**no blocker**); privacy/architecture not run (pure Foundation, no I/O/state). Applied: **S2** disjoint-endpoint clamp (overlap was halving the span), **S1/N1** doc honesty on step transients + the downstream cross-check constraint, `.temporarilyUnavailable` test. **Deferred:** N2 absurd-altitude cap (harmless — supporting-only) + on-device calibration. WG-066 ADR in DECISIONS. |
| WG-067 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 363 tests pass (0 failures), 0 warnings, swiftlint 0 (`MotionDomain` Foundation-only), swift-format clean, tracking OK. `MovementEpisodeTests` (15): **continuous walking → one sustained episode** and intermittent **bed movement → only short episodes** (the canonical pair); **two streams merge by timestamp** (interleaved pedometer + activity → one episode; steps only from the pedometer stream); a **pause splits** episodes; **stale / far-future / non-finite-timestamp / still** observations are dropped; a **NaN `now` fails closed** (empty, review B1); a **cumulative reset freezes accounting** at the pre-reset run and **interleaved resets can't inflate** steps (review S1: `[100,5,105,5,110]` → 0); the pedometer/activity **mappers** map on-foot movement only; empty / single-observation → no episode. | On device (**UAT CP-C/CP-D**, via the challenge): confirm a real continuous walk yields one sustained episode while lying-in-bed tossing does not, and a stale/replayed observation stream can't form a fresh episode (freshness gate) — exercised by the WG-068/069 challenge runtime → `RELEASE_CHECKLIST.md`. | Added `Sources/MotionDomain/MovementEpisode.swift`: `MovementSource`, `MovementObservation` (+ `from(pedometer:)`/`from(activity:)` mappers), `MovementEpisode`, and the pure `MovementEpisodeBuilder` (+ `Config`). **Merges multiple streams by timestamp** (flatten + stable sort), **segments by pause gaps**, and applies the **freshness gate WG-064 deferred** (drops stale/future/non-finite/still). **Reset-safe**: `steps(in:)` freezes on a counter decrease (never inflates); `stepCount` is advisory, not a gate — `duration` + observation **density** (`observationCount`/`duration`) are the anti-cheat signal WG-069 uses. Review: `motion-red-team` (privacy/architecture no surface). Fixed **B1** (NaN-`now` fail-closed — the Date-NaN-blind bounds hole) and **S1** (freeze-on-reset, non-inflatable + honest docstring); documented **S2** (density derivable for WG-069) and **N2** (a future altitude/deviceMotion mapper must not set `isMoving = true` — supporting-only, keeps WG-066's "lone altitude never passes"). **Handoff:** WG-069 owns the cadence-variance + duration/density gate (an emitted episode isn't itself "movement-grade"). WG-067 ADR in DECISIONS. |
| WG-068 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 381 tests pass (0 failures), 0 warnings, swiftlint 0 (`MotionDomain` Foundation-only), swift-format clean, tracking OK. `WakeChallengeTests` (18): the happy path reaches `.passed` and **only `.passed` permits alarm dismissal** (exhaustive over all 7 phases); **timeout / fail / unavailable all keep the alarm active** (#21), `.unavailable` reachable from idle/starting/active; **only valid transitions change state** (invalid events are no-ops returning false; a passed/terminal challenge is stable except `.reset`); progress is **monotonic (peak-based)** and **inflation-proof** — a **replayed** cumulative can't raise the peak, **reset-spam** (`[0,10,0,10,0,10]`) yields 10 not 60, and a **near-`Int.max`** span **saturates without trapping**; a **pause safely resets** to zero; and `ChallengeProgress` clamps `required` ≥ 1 (a zero target can't auto-pass). | On device (**UAT CP-C/CP-D**, via WG-069): a genuinely-walked challenge passes and lets the user dismiss; a timed-out / interrupted / sensor-unavailable challenge keeps the alarm ringing and offers the accessible alternative → `RELEASE_CHECKLIST.md`. | Added `Sources/MotionDomain/WakeChallenge.swift`: `WakeChallengePhase` (String-raw #27; `permitsAlarmDismissal` true for `.passed` **only**), `ChallengeProgress`, `WakeChallengeEvent`, and the pure `WakeChallengeMachine`. Deterministic (reads no clock, no framework, **cannot call AlarmKit / mutate persistence** — reports a phase only); every non-pass terminal keeps the alarm active (#21). **Progress = peak − baseline over cumulative counts** → monotonic + inflation-proof by construction. Reviews: `alarm-safety-reviewer` + `motion-red-team` (both — safety-critical). Both confirmed only-`.passed`-dismisses + terminal stability + determinism, and both found the same **BLOCKER** (an additive `advance` **trapped on `Int.max` overflow** — a crash on the dismissal path) + **SHOULD-FIX** (a trusting raw *delta* was inflatable). **Both fixed** by redesigning to cumulative peak−baseline (overflow-saturating, replay/reset-inflation unrepresentable) + adversarial tests. **Handoffs:** WG-069 feeds `observedProgress(cumulative:)` from verified episodes + drives timeout (owns cadence anti-cheat); **WG-073 must gate ring-stop on `permitsAlarmDismissal`**, not a raw notification action (#24). WG-068 ADR in DECISIONS. |
| WG-069 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 395 tests pass (0 failures), 0 warnings, swiftlint 0 (`MotionDomain` Foundation-only), swift-format clean, tracking OK. `WalkVerificationTests` (14): a **validated walking episode passes**; a **short movement** (< 10 s), a **stationary/stepless pickup**, an **empty** episode list, a **sparse** episode (mean gap > 2 s), a **single-observation** episode, and a **non-finite / negative-duration** episode each **fail** (never a false pass); thresholds are **configurable only within safe bounds** — lenient (2 / 1 / 100) clamps to the floors (10 / 10 / 2.0), stricter is allowed, ceilings cap (500→120, 999→200), `NaN`/`Infinity` fall to safe defaults, and the **ten-second floor holds even under a lenient config**. | On device (**UAT CP-C/CP-D**): a genuine ~10 s walk passes; standing still / a quick pickup / a brief movement does not — and (once WG-070 lands) a rhythmic shake does not → `RELEASE_CHECKLIST.md`. | Added `Sources/MotionDomain/WalkVerification.swift`: `WalkVerification` (`.passed`/`.insufficient` — **no `.failed`**, so a movement inference alone never fails a challenge, #21), `WalkRequirements` (one-directional safe-bounds clamp — a challenge can be stricter, never weaker; ten-second floor guaranteed), and the pure `WalkVerifier` (three conjunctive gates: duration / step floor / mean-gap density). Reviews: `motion-red-team` + `alarm-safety-reviewer` (both — safety-critical). Alarm-safety: **no false pass**, floor unweakenable, determinism + accessible-alternative preserved (27 hostile probes). **HONESTY (B1):** WG-069 gates only the *trivial* fakes — a **regular-cadence shake that produces pedometer steps still passes** (CMPedometer counts shakes as steps; step *regularity* isn't measured here). **That shake/replay defense is WG-070's — the headline anti-cheat threat is NOT closed by WG-069.** Applied: honest docs, **S3** fail-closed `duration.isFinite && >= 0` guard, and **S1/S2** doc reconciliation (`stepCount` used as an inflation-proof lower bound only; mean-gap is a coarse backstop over WG-067's `pauseGap`). **Handoffs:** WG-070 owns cadence-regularity; the WG-068 integrator must map `.insufficient` → keep-waiting, never a stop/fail. WG-069 ADR in DECISIONS. |
| WG-070 | Complete | `yeshdev1/wakeguard-parent-bootstrap` | `make ci-fast` green: 410 tests pass (0 failures), 0 warnings, swiftlint 0 (`MotionDomain` Foundation-only), swift-format clean, tracking OK. `CadenceRegularityTests` (15): a **plausibly-paced, naturally-varied gait corroborates**; **rapid irregular motion fails** — an alternating series → `.tooErratic`, a bursty shake → `.implausibleTiming`, too-fast/too-slow timing → `.implausibleTiming`; a **metronomic** series → `.tooRegular`; **too few** intervals and **non-finite** intervals → `.tooFewSteps`; reconstruction **skips duplicate and reset samples** (so **20 duplicates padded onto a real walk still yield too few intervals** — duplicates cannot inflate); end-to-end a **real walk stream corroborates** and a **bursty shake stream fails**; and — pinned honestly — a **regular ~2/s paced shake passes** (`testPacedShakePassesAsKnownLimitation`), the known CV-model residual. | On device (**UAT CP-C/CP-D**, WG-075 calibration): confirm the "**shaking alone does not pass**" matrix line once the challenge wires the cadence verdict, and calibrate the bands (treadmill / slow / elderly false-positives) → `RELEASE_CHECKLIST.md`. | Added `Sources/MotionDomain/CadenceRegularity.swift`: `CadenceVerdict` (String-raw #27; only `.plausibleGait` corroborates — a rejection is non-authoritative, #21), `CadenceThresholds` (calibratable), and the pure `CadenceRegularity` (reconstruct inter-step intervals from cumulative-step deltas; classify by per-step timing band + coefficient of variation). Review: `motion-red-team`. **Meets acceptance:** rapid *irregular* motion fails; duplicates/resets + constant-cadence replays can't inflate. **Honestly documented limitations (criterion 3):** a *regular/paced* shake passes (CV-only model — deferred to WG-075; safety holds via #19 multi-signal + #20, this is corroboration only, not a standalone pass), exactly-periodic alternation passes (no autocorrelation — WG-075), jittered fabricated replay is only defeated at the adapter injection boundary (WG-063), and `minimumIntervals=8` is coupled to CMPedometer's ~1 Hz delivery. **Corrected false-positive set:** treadmill (`.tooRegular`) + slow/elderly > 1.2 s (`.implausibleTiming`) are the real rejections (a limp passes), each mitigated by the always-available accessible alternative. Applied red-team S3/S5/N3 (replay-scope + false-positive doc corrections + paced-shake pin). **Handoffs:** WG-071/073 wire the verdict in; WG-075 owns calibration + the regular-shake/periodicity follow-up. WG-070 ADR in DECISIONS. |
