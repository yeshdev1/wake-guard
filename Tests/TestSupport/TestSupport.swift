/// Namespace for deterministic test doubles — fakes, `TestClock`,
/// `DeterministicIDGenerator`, motion trace players, and friends.
///
/// Populated in WG-007 (deterministic test-support module). For now it is an
/// empty marker so the folder exists and the test target compiles it.
enum TestSupport {}
